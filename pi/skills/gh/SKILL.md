---
name: gh
description: Patterns for invoking the GitHub CLI (gh) from agents. Placeholder — replaced during the image build.
---

This file is a placeholder. During the image build it is replaced with the
`gh` CLI skill (`skills/gh/SKILL.md`) pinned to the `GH_VERSION` installed in
`docker/Dockerfile`, fetched from https://github.com/cli/cli.
